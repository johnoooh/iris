export default function PrivacyStatement() {
  return (
    <div className="bg-parchment-100 border-b border-parchment-300 px-6 py-2.5">
      <p className="text-xs text-parchment-800">
        🔒 Your information never leaves your device. IRIS collects no data, uses no cookies,
        requires no account, and has no tracking. The only network request is a structured search
        query to ClinicalTrials.gov&apos;s public database.
      </p>
    </div>
  )
}
